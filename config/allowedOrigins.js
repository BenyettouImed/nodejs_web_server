const allowedOrigins = ['https://www.yoursite.com', 'http://127.0.0.1:5500', 'http://localhost:3500']// the sites that will pass the cors and that access our backend, the sencond one can represent our frontend

module.exports = allowedOrigins;